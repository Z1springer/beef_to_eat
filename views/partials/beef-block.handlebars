<li>
	{{name}}

	<button class="eat-it" data-id="{{id}}" data-eaten="{{consumed}}">
		{{#if consumed}}Eat{{else}}Want a Salad?{{/if}}
	</button>

	<button class="delete-beef" data-id="{{this.id}}">DELETE!</button>
</li>